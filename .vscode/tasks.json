{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "camera_live build",
            "type": "shell",
            "command": "cargo build",
            "problemMatcher": [
                "$rustc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}/camera_live",
            }
        }
    ]
}
